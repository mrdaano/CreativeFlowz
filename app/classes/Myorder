<?php

class myOrder {

}

public function getOrder($id, $user_id, $created_at)
	$order = Database::start()->get('*', 'order', array(
			array('user_id', '=', $_SESSION['_user']['id'])
		))->results();